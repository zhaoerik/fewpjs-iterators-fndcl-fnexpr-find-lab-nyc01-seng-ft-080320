const testVar = {}

function testFunc() {
  return "hi"
}

const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(array) {
  array.find(object => {
    if (object.result === "W") {
      return object.year
    } else {
      return undefined
    }
  })
}